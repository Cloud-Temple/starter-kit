# === Framework MCP ===
# Le SDK v2 sépare les types du SDK : garder les deux versions identiques.
mcp[cli]==2.1.1
mcp-types==2.1.1
# Transport requis par le client Streamable HTTP du SDK MCP v2.
httpx2==2.12.0
uvicorn>=0.32.0

# === Configuration ===
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# === CLI + Shell ===
click>=8.1
prompt-toolkit>=3.0
rich>=13.0

# === HTTP Client ===
httpx>=0.27

# === S3 Token Store (optionnel — si S3_ENDPOINT_URL configuré) ===
boto3>=1.34

# === JWT / OIDC (optionnel — activé si JWT_ISSUER est configuré) ===
# RS256/RS512 + ECDSA via le backend cryptography.
# L'extra PyJWT reconnu est ``crypto`` (pas ``cryptography``).
PyJWT[crypto]>=2.10.1
